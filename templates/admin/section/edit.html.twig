{% extends 'admin/base_admin.html.twig' %}

{% block title %}Edition - Section
{% endblock %}

{% block body %}
	<section class="container-fluid">
		<h1>Edition section :
			{{ section.label }}</h1>

		<div class="content">
			{{ include('admin/section/_form.html.twig', {'button_label': 'Update'}) }}
			<a class="btn btn-primary" href="{{ path('app_ressource_index') }}">Retour</a>
		</div>
	</section>
{% endblock %}
