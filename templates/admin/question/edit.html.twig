{% extends 'admin/base_admin.html.twig' %}

{% block title %}Edition Question
{% endblock %}

{% block body %}
	<section class="container-fluid">
		<h1>Edition question :
			{{ question.label }}
		</h1>

		<div class="content">
			{{ include('admin/question/_form.html.twig', {'button_label': 'Update'}) }}
			<a class="btn btn-primary" href="{{ path('app_question_index') }}">Retour</a>
		</div>
	</section>


{% endblock %}
