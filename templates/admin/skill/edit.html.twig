{% extends 'admin/base_admin.html.twig' %}

{% block title %}Edition - Compétence
{% endblock %}

{% block body %}
	<section class="container-fluid">
		<h1>Edition compétence :
			{{skill.name}}</h1>

		<div class="content">
			{{ include('admin/skill/_form.html.twig', {'button_label': 'Update'}) }}
			<a class="btn btn-primary" href="{{ path('app_skill_index') }}">Retour</a>
		</div>
	</section>


{% endblock %}
