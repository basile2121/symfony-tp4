<form method="post" action="{{ path('app_high_school_delete', {'id': high_school.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ high_school.id) }}">
	<button class="btn btn-danger">Supprimer</button>
</form>
