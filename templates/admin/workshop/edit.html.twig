{% extends 'admin/base_admin.html.twig' %}

{% block title %}Edition Atelier
{% endblock %}

{% block body %}
	<section class="container-fluid">
		<h1>Edition Atelier :
			{{workshop.name}}</h1>

		<div class="content">
			{{ include('admin/workshop/_form.html.twig', {'button_label': 'Update'}) }}
			<a class="btn btn-primary" href="{{ path('app_workshop_index') }}">Retour</a>
		</div>

	</section>
{% endblock %}
