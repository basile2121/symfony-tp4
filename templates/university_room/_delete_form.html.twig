<form method="post" action="{{ path('app_university_room_delete', {'id': university_room.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ university_room.id) }}">
    <button class="btn">Delete</button>
</form>
