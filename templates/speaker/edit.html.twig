{% extends 'base.html.twig' %}

{% block title %}Edit Speaker{% endblock %}

{% block body %}
    <h1>Edit Speaker</h1>

    {{ include('speaker/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-primary" href="{{ path('app_speaker_index') }}">Retour</a>

    {{ include('speaker/_delete_form.html.twig') }}
{% endblock %}
